# RAWRS — development dependencies.
#
# Pinned so test behaviour is reproducible across machines. Test collection
# and marker semantics are pytest-version-sensitive, so an unpinned pytest
# can change which tests run without any change to RAWRS itself.

-r requirements.txt
pytest==9.1.1
pytest-cov==7.1.0
